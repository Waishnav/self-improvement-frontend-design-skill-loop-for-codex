Version 27 completed.

- Implemented a fresh Vite + React + TypeScript + Tailwind + Bun app in `t4-canvas/`.
- Built the root navigation page plus five distinct T4 Canvas landing page routes at `/1` through `/5`.
- Validation passed with `bun run lint` and `bun run build`.
- Captured desktop, tablet, mobile, and laptop-alias screenshots for routes `/1` to `/5`.
- Capture ran against `http://127.0.0.1:4001` because `4000` was already occupied by an unrelated local Vite process outside this experiment.

Authoritative run notes are recorded in:

- `experiments/version-27/README.md`
- `experiments/version-27/CRITQUES.md`
